# Data

`scripts/make_sample_data.R` creates `data/sample_panel.csv`, a synthetic panel with
the same schema expected by the analysis scripts.

Licensed WRDS-derived files should be stored locally under `data/raw/`. That directory
is intentionally excluded from Git.
